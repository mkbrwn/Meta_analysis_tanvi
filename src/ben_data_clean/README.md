This is Ben's R files

Within cleaning_trial directory, there are 4 further directories
1. data - age
2. data - average
3. data - proportion
4. manual clean requirement

For each of the 'data' directories there are at least 3 subdirectories
a. scripts: the R scripts produced by claude to clean the data
b. cleaned: the cleaned spreadhseet produced by each R script
c. prompt/errors: errors I had noted after a brief review of the data, used in a prompt to iterate a new R script
    [Note this is absent for 'average data' because the prompts were done with claude code in VS code, rather than through a generic claude chat on the web.]

The manual clean requirement has 2 documents
i. a longer document with claude's fuller output copied in.
ii. a summary document produced by claude.